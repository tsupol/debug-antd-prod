import React from 'react'
import page from '../../hocs/page'
import { Heading1, WmLink, WmUpload, WmButton } from '../../components/wm'

class Register extends React.Component {
  render () {
    return (
      <div>
        nothing
      </div>
    )
  }
}

export default page(Form.create()(Register))